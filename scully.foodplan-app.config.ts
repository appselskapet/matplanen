import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./projects/foodplan-app/src",
  projectName: "foodplan-app",
  outDir: './dist/static',
  routes: {
  }
};